/**
 * Category Page
 * Displays the Category Grid section
 */

import CategoryGrid from '../components/sections/CategoryGrid';
import './Category.css';

const Category = () => {
    return (
        <main className="category-page">
            <CategoryGrid />
        </main>
    );
};

export default Category;
